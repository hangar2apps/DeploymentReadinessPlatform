# Screenshots

Drop the README screenshots here with these exact filenames (referenced by the
root [README.md](../../README.md) "Screenshots" section):

| File | Capture |
|------|---------|
| `login.png` | The persona login screen (four cards) |
| `service-member.png` | A step of the Service Member assessment wizard |
| `provider.png` | The Provider review queue (red-flagged items first) or a detail drawer |
| `commander.png` | The Commander readiness dashboard (KPI cards + per-company bars) |

Capture from the live demo or a local `npm run dev`. PNG preferred; aim for a
consistent width (~1400px) so the 2×2 grid looks even.
